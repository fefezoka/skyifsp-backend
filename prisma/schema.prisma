generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["jsonProtocol"]
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator erd {
  provider = "prisma-erd-generator"
  output   = "./ERD.png"
}

model Airplane {
  id      String   @id @default(uuid())
  code    String   @unique
  seats   Int
  plane   String
  Flights Flight[]
}

model Airport {
  id                 String   @id @default(uuid())
  code               String   @unique
  country            String
  state              String
  city               String
  airport            String
  latitude           String
  longitude          String
  originFlights      Flight[] @relation("outward")
  destinationFlights Flight[] @relation("outbound")
}

model Flight {
  id            String   @id @default(uuid())
  departureDate DateTime
  arrivalDate   DateTime
  origin        Airport  @relation("outward", fields: [originId], references: [id], onDelete: Cascade)
  destination   Airport  @relation("outbound", fields: [destinationId], references: [id], onDelete: Cascade)
  airplane      Airplane @relation(fields: [airplaneId], references: [id], onDelete: Cascade)
  originId      String
  destinationId String
  airplaneId    String
}
